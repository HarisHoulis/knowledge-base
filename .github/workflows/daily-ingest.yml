name: Daily Ingest

on:
  schedule:
    - cron: "0 6 * * *"
  workflow_dispatch:
    inputs:
      dry_run:
        description: "Preview changes without committing or pushing"
        type: boolean
        required: false
        default: false
      limit:
        description: "Maximum entries to process per source"
        type: number
        required: false
        default: 0

jobs:
  ingest:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - uses: actions/setup-python@v5
        with:
          python-version: "3.12"
          cache: pip

      - name: Install dependencies
        run: |
          pip install --upgrade pip
          pip install -e ".[dev]"

      - uses: actions/cache@v4
        with:
          path: ~/.kb-pipeline/state.json
          key: kb-state-v1

      - name: Run daily ingest
        env:
          DEEPSEEK_API_KEY: ${{ secrets.DEEPSEEK_API_KEY }}
        run: |
          git config user.email "github-actions[bot]@users.noreply.github.com"
          git config user.name "github-actions[bot]"
          ARGS=""
          if [[ "${{ inputs.dry_run }}" == "true" ]]; then
            ARGS="$ARGS --dry-run"
          fi
          if [[ -n "${{ inputs.limit }}" && "${{ inputs.limit }}" != "0" ]]; then
            ARGS="$ARGS --limit=${{ inputs.limit }}"
          fi
          bash scripts/daily-ingest.sh $ARGS


